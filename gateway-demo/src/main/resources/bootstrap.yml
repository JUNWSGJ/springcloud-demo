server:
  port: 8080

spring:
  application:
    name: gateway-demo
  cloud:
    discovery:
      locator:
        enabled: true
        lower-case-service-id: true
    nacos:
      discovery:
        namespace: springcloud-demo
        server-addr: localhost:8848
#        username: nacos
#        password: nacos
#logging:
#  level:
#    ROOT: debug
