server:
  port: 8081

spring:
  application:
    name: service-a
  cloud:
    nacos:
      discovery:
        namespace: springcloud-demo
        server-addr: localhost:8848
logging:
  level:
    ROOT: debug
